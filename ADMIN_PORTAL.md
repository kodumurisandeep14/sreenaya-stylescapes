# Admin Portal

A separate admin portal repository has been created at:

`/Users/ksiri/Documents/SreenayaS/stylescapes-admin`

It contains a standalone React + Vite app plus a secure Express API proxy for Supabase service role insertions.

To run it:

```bash
cd /Users/ksiri/Documents/SreenayaS/stylescapes-admin
npm install
npm run dev
```

Then open `http://localhost:5173`.
